#include "utils.h"

/* File path */
#define FILE_PATH "/etc/myfact_config.txt"


/** @brief  methods to calculate the number of factorial from argument.
 *  @param aArgc number of arguments of user.
 *  @param aArgv arguments.
 */
void mode_1(int aArgc,char* aArgv[]);


/** @brief  methods to calculate the number of factorial from input line (with scanf).
 */
void mode_2();